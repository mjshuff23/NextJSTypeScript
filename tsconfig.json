{
  "compilerOptions": {
    // Specifies the library files to be included in the compilation. Here you have "dom", "dom.iterable", and "esnext", which means the TypeScript compiler will include type definitions for:
    // - DOM
    // - DOM iterable manipulations
    // - ECMAScript features that are in the proposal stage.
    "lib": [
      "dom",
      "dom.iterable",
      "esnext"
    ],
    // allowJs: When set to true, allows JavaScript files to be compiled.
    "allowJs": true,
    // skipLibCheck: Skips type-checking of declaration files (.d.ts files).
    "skipLibCheck": true,
    // strict: Enables all strict type-checking options. In your configuration, it's set to false.
    "strict": false,
    // noEmit: Disables the generation of output files (like .js files). This is useful when you're using Next.js, which takes care of this step.
    "noEmit": true,
    // incremental: Enables incremental compilation by reading/writing information from cache to speed up subsequent compilations.
    "incremental": true,
    // esModuleInterop: Allows default imports from modules with no default export.
    "esModuleInterop": true,
    // module: Specifies the module code generation method. esnext means it will generate code for ESNext/ECMAScript 2015+ modules.
    "module": "esnext",
    // moduleResolution: Specifies module resolution strategy. node is used for Node.js-like module resolution where it looks inside node_modules.
    "moduleResolution": "node",
    // resolveJsonModule: Allows the import of JSON files as modules.
    "resolveJsonModule": true,
    // isolatedModules: Ensures that every file gets transpiled as a separate module to align with the ECMAScript standard.
    "isolatedModules": true,
    // jsx: Specifies the JSX code generation. preserve means JSX syntax is preserved to be processed by another transpiler, like Babel.
    "jsx": "preserve"
  },
  // Specifies which files should be included for the TypeScript compiler to process:
  // - next-env.d.ts: A type declaration file generated by Next.js.
  // - **/*.ts: Includes all .ts files in all folders and subfolders.
  // - **/*.tsx: Includes all .tsx files in all folders and subfolders.
  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx"
  ],
  // Specifies which files or folders should be excluded from the compilation process:
  // - node_modules: Excludes the node_modules directory to speed up the compilation process.
  "exclude": [
    "node_modules"
  ]
}
